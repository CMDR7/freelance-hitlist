name = "fl-hl-intelligence-api"
main = "worker.js"
compatibility_date = "2026-08-17"

# V2.4 intentionally ships without a live database binding.
# D1 will be introduced when normalized ingestion begins.
#
# Example future binding:
# [[d1_databases]]
# binding = "DB"
# database_name = "fl-hl"
# database_id = "<created-in-cloudflare>"

[vars]
API_VERSION = "2.4.0"
