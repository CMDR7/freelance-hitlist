name = "fl-hl-intelligence-api"
main = "worker.js"
compatibility_date = "2026-08-17"

# V2.7 keeps persistence disabled while normalization and deduplication are validated.
# D1 will be introduced at the appropriate persistence stage.
#
# Example future binding:
# [[d1_databases]]
# binding = "DB"
# database_name = "fl-hl"
# database_id = "<created-in-cloudflare>"

[vars]
API_VERSION = "2.7.0"
